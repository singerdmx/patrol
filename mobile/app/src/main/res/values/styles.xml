<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.Light">
        <item name="android:windowTitleBackgroundStyle">@style/titleBarBackground</item>
        <item name="android:windowTitleSize">52dip</item>
    </style>

    <style name="titleBarBackground">
        <item name="android:background">@drawable/background_cyan_no_corner</item>
    </style>

</resources>
